/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Config file for defining DSL2 per module options and publishing paths
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Available keys to override module options:
        ext.args   = Additional arguments appended to command in module.
        ext.args2  = Second set of arguments appended to command in module (multi-tool modules).
        ext.args3  = Third set of arguments appended to command in module (multi-tool modules).
        ext.prefix = File name prefix for output files.
        ext.when   = When to run the module.
----------------------------------------------------------------------------------------
*/
// Pave configs

process {

    withName: 'MSKILABORG_NFJABBA:NFJABBA:VCF_PAVE_SOMATIC:PAVE' {
        ext.when         = { params.tools && params.tools.split(',').contains('pave') }
        publishDir       = [
                mode: params.publish_dir_mode,
                path: { "${params.outdir}/variant_annotations/PAVE/somatic/${meta.id}/" },
                pattern: "*{.vcf*,.tbi*}"
        ]
    }
    withName: 'MSKILABORG_NFJABBA:NFJABBA:VCF_PAVE_GERMLINE:PAVE' {
        ext.when         = { params.tools && params.tools.split(',').contains('pave') }
        publishDir       = [
                mode: params.publish_dir_mode,
                path: { "${params.outdir}/variant_annotations/PAVE/germline/${meta.id}/" },
                pattern: "*{.vcf*,.tbi*}"
        ]
    }

    withName: 'MSKILABORG_NFJABBA:NFJABBA:VCF_SNPEFF_SOMATIC:SNPEFF_SNPEFF' {
        ext.when         = { params.tools && params.tools.split(',').contains('snpeff') }
        publishDir       = [
                mode: params.publish_dir_mode,
                path: { "${params.outdir}/variant_annotations/SNPEFF/somatic/${meta.id}/" },
                pattern: "*{.vcf*,.txt*,.html*,.csv*}"
        ]
    }
    withName: 'MSKILABORG_NFJABBA:NFJABBA:VCF_SNPEFF_SOMATIC:SNPEFF_VCF_TO_BCF' {
        ext.when         = { params.tools && params.tools.split(',').contains('snpeff') }
        publishDir       = [
                mode: params.publish_dir_mode,
                path: { "${params.outdir}/variant_annotations/SNPEFF/somatic/${meta.id}/" },
                pattern: "*{.bcf*}"
        ]
    }
    withName: 'MSKILABORG_NFJABBA:NFJABBA:VCF_SNPEFF_GERMLINE:SNPEFF_SNPEFF' {
        ext.when         = { params.tools && params.tools.split(',').contains('snpeff') }
        publishDir       = [
                mode: params.publish_dir_mode,
                path: { "${params.outdir}/variant_annotations/SNPEFF/germline/${meta.id}/" },
                pattern: "*{.vcf*,.txt*,.html*,.csv*}"
        ]
    }
    withName: 'MSKILABORG_NFJABBA:NFJABBA:VCF_SNPEFF_GERMLINE:SNPEFF_VCF_TO_BCF' {
        ext.when         = { params.tools && params.tools.split(',').contains('snpeff') }
        publishDir       = [
                mode: params.publish_dir_mode,
                path: { "${params.outdir}/variant_annotations/SNPEFF/germline/${meta.id}/" },
                pattern: "*{.bcf*}"
        ]
    }
}
